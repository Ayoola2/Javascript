var list = ["tiger", "Cat", "bear", "bird"];
console.log(list[0]); //This is how to access information in the array
list.shift(); // This shifts tiger away from the array
list.pop(); // This pops bird away from the array
list.push("Elephant"); // This pushes elephant into the array
list.concat(["Bee", "Deer"]); // This adds Bee and deep to the array
list.sort() //sorts the list alphabetically

var myList = ["Dragon", "Whale", "shark", "bird"];

var myNewList = myList.concat(["Snake"])

// Here we have two different arrays myList and myNewList


// array within an array

var list = [
    ["Tiger", "Dog", "Lion"]
];
console.log(list[0] [1]);



